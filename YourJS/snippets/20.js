storeSnippet({"id":20,"name":"walk() - Traverse Array\/Object Values","description":"Traverses all of the properties of an object or all of the values of an array or array-like object.  This is similar to the native forEach() function.","js":"function walk(arrOrObj, fn, opt_walkAll) {\r\n  var done, c = 0, k = 0, l, endWalk = function() { done = arguments; },\r\n    caller = function(k) { c++; fn.call(endWalk, arrOrObj[k], k, arrOrObj); };\r\n  if (isArrayLike(opt_walkAll)) {\r\n    for (l = opt_walkAll.length; k < l; k++) {\r\n      if (has(opt_walkAll, k)) {\r\n        caller(opt_walkAll[k]);\r\n        if (done) { break; }\r\n      }\r\n    }\r\n  }\r\n  else if (isArrayLike(arrOrObj)) {\r\n    for (l = arrOrObj.length; k < l; k++) {\r\n      if (opt_walkAll || has(arrOrObj, k)) {\r\n        caller(k);\r\n        if (done) { break; }\r\n      }\r\n    }\r\n  }\r\n  else {\r\n    for (k in arrOrObj) {\r\n      if (opt_walkAll || has(arrOrObj, k)) {\r\n        caller(k);\r\n        if (done) { break; }\r\n      }\r\n    }\r\n  }\r\n  return done && done.length ? done : c;\r\n}","post":"<h2><code>walk(...)<\/code> API Documentation<\/h2>\r\n<div style=\"margin: 0 30px 30px;\">\r\n    <h3>Description<\/h3>\r\n    <div>Traverses the specified array the same way that you would with a <code>for<\/code> loop or a <code>for...in<\/code> loop for an object.<\/div>\r\n    \r\n    <h3>Parameters<\/h3>\r\n    <ol>\r\n        <li>\r\n            <b><code>arrOrObj<\/code> {Array|Object}:<\/b><br \/>\r\n            The array or object that is to be traversed.\r\n        <\/li>\r\n        <li>\r\n            <b><code>fn<\/code> {function(value, key, arrOrObj)}:<\/b><br \/>\r\n            The callback to be called for each value in the array or object.  If you want the current iteration to be the last iteration you can call <code class=\"language-javascript\">this()<\/code>.\r\n        <\/li>\r\n        <li>\r\n            <b><code>opt_walkAll<\/code> {boolean|Array}:<\/b><br \/>\r\n            Optional.  Defaults to <code>false<\/code>.  If <code>true<\/code> all array keys or object properties will be traversed even if they aren't owned by <code>arrOrObj<\/code> (which is determined by <code>hasOwnProperty()<\/code>.  If this is an array, the values that will be traversed will be those found at the indices\/names stored in the array.\r\n        <\/li>\r\n    <\/ol>\r\n    \r\n    <h3>Returns<\/h3>\r\n    <div>If <code class=\"language-javascript\">this(...)<\/code> was called within <code>fn(...)<\/code>, the arguments object of the arguments passed will be returned.  Otherwise, returns the number of properties that were actually traversed.<\/div>\r\n<\/div>","required_ids":{"81":"isArrayLike() & toArray()"},"tags":["Array","Function","Object"],"variables":["walk"]});