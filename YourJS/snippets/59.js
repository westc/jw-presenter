storeSnippet({"id":59,"name":"flatten()","description":"Turns an array with sub-arrays into an array without sub-arrays.  Provides to option to do this recursively until even the sub-arrays of sub-arrays and all subsequent sub-arrays are merged into one array in the order found.","js":"function flatten(arr, opt_deep) {\r\n  arr = slice(arr);\r\n  for (var e, i = arr.length; i--;) {\r\n    if (typeOf(e = arr[i], 'Array')) {\r\n      arr.splice.apply(arr, [i, 1].concat(opt_deep ? flatten(e, 1) : e));\r\n    }\r\n  }\r\n  return arr;\r\n}","post":"<h2><code>flatten(...)<\/code> API Documentation<\/h2>\r\n<div style=\"margin: 0 30px 30px\">\r\n  <div>Turns an array with sub-arrays into an array without sub-arrays.  Provides the option to do this recursively until even the sub-arrays of sub-arrays and all subsequent sub-arrays are merged in to make one large array.<\/div>\r\n  \r\n  <h3>Parameters<\/h3>\r\n  <ol>\r\n    <li>\r\n      <b><code>arr<\/code> {Array}:<\/b><br \/>\r\n      The array that will be duplicated and whose sub-arrays will be merged into the duplicated array in the place in which they are found.\r\n    <\/li>\r\n    <li>\r\n      <b><code>opt_deep<\/code> {boolean=}:<\/b><br \/>\r\n      Optional.  Defaults to <code class=\"language-javascript\">false<\/code>.  If <code class=\"language-javascript\">true<\/code> even sub-arrays of sub-arrays will be merged into the returned duplicate of <code>arr<\/code> in the place they are found so that no sub-arrays will be present in the returned array.  If <code class=\"language-javascript\">false<\/code> only sub-arrays of <code>arr<\/code> will be merged into the returned duplicate of <code>arr<\/code>, leaving any sub-arrays of sub-arrays in as is without recursively mergining them into the returned array.\r\n    <\/li>\r\n  <\/ol>\r\n  \r\n  <h3>Returns<\/h3>\r\n  <div>Returns a duplicate of <code>arr<\/code> where the sub-arrays are merged in place and if <code>opt_deep<\/code> is <code class=\"language-javascript\">true<\/code> even the sub-arrays of sub-arrays and beyond will be merged in making it so no sub-arrays exist.<\/div>\r\n<\/div>","required_ids":{},"tags":["Array"],"variables":["flatten"]});