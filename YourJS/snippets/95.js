storeSnippet({"id":95,"name":"splice()","description":"Duplicates an array or a string and adds and\/or removes one or more values from it.","js":"function splice(arrOrStr, start, opt_length, opt_replacement) {\r\n  opt_length = opt_length != undefined\r\n    ? opt_length < 0\r\n      ? arrOrStr.length - start + opt_length\r\n      : opt_length\r\n    : Infinity;\r\n  start = start < 0 ? Math.max(0, arrOrStr.length + start) : start;\r\n  if (isArrayLike(arrOrStr)) {\r\n    return slice(arrOrStr, 0, start).concat(opt_replacement || [], slice(arrOrStr, start + opt_length));\r\n  }\r\n  return arrOrStr.slice(0, start)\r\n    + (opt_replacement === undefined ? '' : opt_replacement)\r\n    + arrOrStr.slice(start + opt_length);\r\n}","post":"<h2><code>splice(...)<\/code> API Documentation<\/h2>\r\n<div style=\"margin: 0 30px 30px\">\r\n  <div>Duplicates an array or a string and adds and\/or removes one or more values from it.  NOTE:  This is different from the traditional splice function because it doesn't modify the array, but rather it returns a modified duplicate.<\/div>\r\n  \r\n  <h3>Parameters<\/h3>\r\n  <ol>\r\n    <li>\r\n      <b><code>arrOrStr<\/code> {Array|string}:<\/b><br \/>\r\n      The array or string to be duplicated and modified.\r\n    <\/li>\r\n    <li>\r\n      <b><code>start<\/code> {number}:<\/b><br \/>\r\n      The index at which to start modifying <code>arrOrStr<\/code>.  If this number is negative the index will be calculated from the end of <code>arrOrStr<\/code>.\r\n    <\/li>\r\n    <li>\r\n      <b><code>opt_length<\/code> {number=}:<\/b><br \/>\r\n      Optional.  Defaults to <code class=\"language-javascript\">Infinity<\/code>.  Indicates the amount of indices that should be removed from <code>arrOrStr<\/code>.  If negative this will be used as if it were an index counting from the end of <code>arrOrStr<\/code>.\r\n    <\/li>\r\n    <li>\r\n      <b><code>opt_replacement<\/code> {Array|string}:<\/b><br \/>\r\n      Optional.  If specified, this will be inserted at <code>start<\/code>.  If <code>arrOrStr<\/code> is an array, this should be an array of the values to insert at <code>start<\/code>.  If <code>arrOrStr<\/code> is a string, this should be the string to insert at <code>start<\/code>.\r\n    <\/li>\r\n  <\/ol>\r\n  \r\n  <h3>Returns<\/h3>\r\n  <div>The modified duplicate of <code>arrOrStr<\/code>.<\/div>\r\n<\/div>","required_ids":{"81":"isArrayLike() & toArray()"},"tags":["Array","String"],"variables":["splice"]});