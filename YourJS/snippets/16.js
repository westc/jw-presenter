storeSnippet({"id":16,"name":"restParam()","description":"Creates a wrapper function which calls the wrapped function with all of the arguments after a certain point passed in as the last argument as an array.","js":"function restParam(fn, opt_start) {\r\n  opt_start = opt_start == undefined ? fn.length - 1 : opt_start;\r\n  return function() {\r\n    var args = slice(arguments, 0, opt_start);\r\n    args.push(slice(arguments, opt_start));\r\n    return fn.apply(this, args);\r\n  }\r\n}","post":"The concept of a rest parameter was introduced in ES6.  It is basically a the summary of all of the passed parameters after the last normal one.  Imagine that we have a function defined as this:\r\n```javascript\r\nfunction fn(a, b, ...theRest) {\r\n  \/\/ code\r\n}\r\n```\r\n\r\nIf we called `fn(1,2,3,4,5,6)`, `a` would be `1`, `b` would be `2` and `theRest` would be `[3,4,5,6]`.  Now, with `YourJS.restParam()` we can do pretty much the same thing in JS engines that dont allow ES6:\r\n```javascript\r\nYourJS.restParam(function(a, b, theRest) {\r\n}, 2);\r\n```\r\n\r\n<h2><code>restParam(fn [, opt_start=fn.length-1])<\/code> API Documentation<\/h2>\r\n<div style=\"margin: 0 30px 30px;\">\r\n    <h3>Parameters<\/h3>\r\n    <ol>\r\n        <li>\r\n            <b><code>fn<\/code> {Function}:<\/b><br \/>\r\n            The function to call with the normal parameters followed by the rest parameter in its place as specified by <code>opt_start<\/code>.\r\n        <\/li>\r\n        <li>\r\n            <b><code>opt_start<\/code> {number}:<\/b><br \/>\r\n            Optional.  Defaults to <code>fn.length - 1<\/code>.  The index within the parameters passed to the returned function that will start the summary of the rest parameter.\r\n        <\/li>\r\n    <\/ol>\r\n    \r\n    <h3>Returns<\/h3>\r\n    <div>Returns a function that when invoked will call <code>fn<\/code>, but if there are any that have an index greater or equal to <code>opt_start<\/code> they will be passed in as the last parameter which will be an array.<\/div>\r\n<\/div>","required_ids":{},"tags":["Array","ES6\/7","Function"],"variables":["restParam"]});